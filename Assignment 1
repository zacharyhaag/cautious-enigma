#include <iostream>
#include <fstream>
#include <string>
#include <cstring>
#include <iomanip>
using namespace std;

struct PERSON
{
  char Name[20] ;
  float Balance;
  int array[10];

};

int main()
{


  string str1, str2, sname;
  ifstream fin;
  fin.open("data.txt"); //go to titanium. find google doc and make txt file to read from 
  PERSON customer;
  customer.Balance[N]; //needs work here

if (fin.is_open())
cout << "Name\t\tBalance" << endl;
cout << "--------------------------------------" << endl;
{
while(!fin.eof())
{
  fin >> str1;
  fin >> str2;
  sname = str1 + " " + str2;
  strcpy(customer.Name, sname.c_str());


  cout << customer.Name << '\0' << "\t";





}
}
fin.close();



return 0;
}
